generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Items {
    id          Int    @id @default(autoincrement())
    name        String
    colorCode   String
    price       Float
    description String
    imageUrl    String

    @@map("items")
}
